// TODO: Phase 1 - Add necessary using statements for data annotations

namespace TaskManagement.API.Models;

// TODO: Phase 1 - Create TaskItem class with the following properties:
// - Id (int)
// - Title (string, required, max 200 chars)
// - Description (string, optional, max 1000 chars) 
// - IsCompleted (bool)
// - CreatedAt (DateTime, default to UtcNow)
// - CompletedAt (DateTime?, optional)
// - DueDate (DateTime?, optional)
// - Priority (TaskPriority enum, default Medium)
// - Status (TaskStatus enum, default NotStarted)
// - AssignedUserId (int?, optional)

// TODO: Phase 1 - Create TaskPriority enum with values:
// Low = 1, Medium = 2, High = 3, Critical = 4

// TODO: Phase 1 - Create TaskStatus enum with values:
// NotStarted = 1, InProgress = 2, OnHold = 3, Completed = 4, Cancelled = 5
